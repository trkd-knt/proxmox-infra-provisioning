# network interface settings; autogenerated
# Please do NOT modify this file directly, unless you know what
# you're doing.
#
# If you want to manage parts of the network configuration manually,
# please utilize the 'source' or 'source-directory' directives to do
# so.
# PVE will preserve these directives, but will NOT read its network
# configuration from sourced files, so do not attempt to move any of
# the PVE managed interfaces into external files!

auto lo
iface lo inet loopback
# loopback interface

# for management physical interface
auto enx00e04c0a2480
iface enx00e04c0a2480 inet manual
	ovs_type OVSPort
	ovs_bridge ovsbr0
# enx00e04c0a2480 physical interface

# for management interface
auto ovsbr0
iface ovsbr0 inet manual
    address 192.168.100.196/24
    gateway 192.168.100.1
    ovs_type OVSBridge
    ovs_ports enx00e04c0a2480
# OVS base bridge

# for service interface
auto enx00e04c3b0728
iface enx00e04c3b0728 inet manual
	ovs_type OVSPort
	ovs_bridge ovsbr1

# for service base bridge
auto ovsbr1
iface ovsbr1 inet manual
	ovs_type OVSBridge
	ovs_ports enx00e04c3b0728
# OVS base bridge

auto vlan10
iface vlan10 inet manual
	address 10.0.10.11/24
	ovs_type OVSIntPort
	ovs_bridge ovsbr1
	ovs_options tag=10
# OVS VLAN 10 (for Front Service Segment)

auto vmbr10
iface vmbr10 inet manual
	ovs_type OVSBridge
	ovs_ports vlan10
# OVS Bridge for VLAN 10

auto vlan20
iface vlan20 inet manual
	address 10.0.20.11/24
	ovs_type OVSIntPort
	ovs_bridge ovsbr1
	ovs_options tag=20
# OVS VLAN 20 (for Backend Service Segment)

auto vmbr20
iface vmbr20 inet manual
	ovs_type OVSBridge
	ovs_ports vlan20
# OVS Bridge for VLAN 20

auto vlan30
iface vlan30 inet manual
	address 10.0.30.11/24
	ovs_type OVSIntPort
	ovs_bridge ovsbr1
	ovs_options tag=30
# OVS VLAN 30 (for Operation Segment)

auto vmbr30
iface vmbr30 inet manual
	ovs_type OVSBridge
	ovs_ports vlan30
# OVS Bridge for VLAN 30
