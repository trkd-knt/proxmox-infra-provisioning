---
- name: apt update
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: install openvswitch-switch
  apt:
    name: openvswitch-switch
    state: present

- name: Open vSwitch status check
  systemd:
    name: openvswitch-switch
    enabled: yes
    state: started
